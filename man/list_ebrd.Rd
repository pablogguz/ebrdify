% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_ebrd.R
\name{list_ebrd}
\alias{list_ebrd}
\title{List EBRD economies}
\usage{
list_ebrd(what = c("both", "iso3c", "name"), group = NULL)
}
\arguments{
\item{what}{What to return: \code{"both"} (default) a data frame with \code{iso3c} and
\code{name} columns; \code{"iso3c"} a character vector of ISO3 codes; \code{"name"} a
character vector of official names.}

\item{group}{Optional. One traditional \code{coo_group} to filter to, e.g.
\code{"Central Asia"}. \code{NULL} (default) returns all economies. See
\code{\link[=list_ebrd_groups]{list_ebrd_groups()}} for the valid values.}
}
\value{
A data frame (\code{what = "both"}) or character vector, ordered
alphabetically by official name (or by code for \code{what = "iso3c"}).
}
\description{
Returns the full list of the 41 EBRD economies — their ISO3 codes, their
official EBRD/Transition Report names, or both — optionally restricted to a
single traditional regional grouping.
}
\examples{
list_ebrd()                       # data frame of all 41 economies
list_ebrd("iso3c")                # just the ISO3 codes
list_ebrd("name", group = "Central Asia")
}
\seealso{
\code{\link[=ebrdify]{ebrdify()}} to classify a dataset and \code{\link[=canonise]{canonise()}} to standardise
country names.
}
