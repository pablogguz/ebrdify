% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonise.R
\name{canonise}
\alias{canonise}
\title{Canonise country names to official EBRD terminology}
\usage{
canonise(x, from = NULL, warn = TRUE)
}
\arguments{
\item{x}{A vector of country identifiers (names, ISO3, or ISO2 codes).}

\item{from}{Format of \code{x}: \code{"country.name"}, \code{"iso3c"}, or \code{"iso2c"}. If
\code{NULL} (default) the format is auto-detected.}

\item{warn}{If \code{TRUE} (default), report identifiers that could not be matched
via \code{\link[=message]{message()}}.}
}
\value{
A character vector the same length as \code{x} of official EBRD/TR names,
with \code{NA} for identifiers that could not be matched. Designed to drop into
\code{dplyr::mutate()}, e.g. \code{mutate(country = canonise(country))}.
}
\description{
Rewrites country identifiers (names, ISO3 or ISO2 codes) to the official
country names used in the EBRD Transition Report. This enforces the TR house
style and the "forbidden names" rules from Annex I of the style guide — for
example:
}
\details{
\itemize{
\item Czech Republic \eqn{\rightarrow} Czechia
\item Palestine \eqn{\rightarrow} West Bank and Gaza
\item Kyrgyzstan \eqn{\rightarrow} Kyrgyz Republic
\item Slovakia \eqn{\rightarrow} Slovak Republic
\item Turkey \eqn{\rightarrow} Türkiye
\item Ivory Coast \eqn{\rightarrow} Côte d'Ivoire
\item Taiwan \eqn{\rightarrow} Taipei China
}

EBRD economies are named from the official Annex I list; other economies fall
back to \code{countrycode}'s English short name, with TR-specific overrides applied
on top (Taipei China, Hong Kong SAR, Macao SAR, Czechia) because \code{countrycode}
returns a non-TR form for those.
}
\examples{
canonise(c("Czech Republic", "Palestine", "Kyrgyzstan", "Taiwan"))
canonise(c("TUR", "SVK", "CIV"), from = "iso3c")
canonise(c("Germany", "Wakanda"))   # non-EBRD pass through; unmatched -> NA
}
\seealso{
\code{\link[=ebrdify]{ebrdify()}} to classify economies and \code{\link[=list_ebrd]{list_ebrd()}} for the full
list of EBRD economies.
}
